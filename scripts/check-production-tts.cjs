(async()=>{const base='https://school-tau-pearl.vercel.app';const get=await fetch(base+'/api/tts');console.log('GET',get.status,await get.json());let success=0;for(const id of ['kongi','tori','nabi','bori']){const r=await fetch(base+'/api/tts',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({characterId:id,text:'안녕하세요. 만나서 반가워요.'})});if(r.ok&&r.headers.get('content-type')?.startsWith('audio/')){const data=Buffer.from(await r.arrayBuffer());const valid=data.toString('ascii',0,4)==='RIFF'&&data.length>44;console.log(id,r.status,r.headers.get('x-tts-voice'),{valid,bytes:data.length,rate:data.readUInt32LE(24)});if(valid)success++;}else console.log(id,r.status,await r.text());}console.log('Valid production audio responses:',success+'/4');process.exitCode=success===4?0:1;})().catch(e=>{console.error(e.message);process.exitCode=1;});
