const {chromium}=require('playwright'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage();await p.goto('http://127.0.0.1:8085');await p.locator('#openJointRoom').click();assert.equal(await p.locator('#jointExerciseRoom iframe,#jointVideo,.joint-loading,.joint-controls').count(),0);const links=p.locator('.joint-video-choice');assert.equal(await links.count(),2);for(const [i,id] of ['uzc_4nnMIz0','7x9F-RQwypc'].entries()){assert.equal(await links.nth(i).getAttribute('href'),'https://www.youtube.com/watch?v='+id);assert.equal(await links.nth(i).getAttribute('target'),'_blank');}await p.locator('#closeJointRoom').click();assert.equal(await p.locator('#jointExerciseRoom').evaluate(e=>e.open),false);console.log('PASS: two direct YouTube links, no embedded/loading area, room closes.');}finally{await b.close();}})().catch(e=>{console.error(e);process.exit(1)});
