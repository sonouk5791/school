const {chromium}=require('playwright'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:390,height:844}});await p.goto('http://127.0.0.1:8085');await p.evaluate(()=>{VoiceManager.isMuted=true;LessonEngine.startLesson('prompt_basic');});assert.equal(await p.locator('.chatgpt-example').count(),1);await p.locator('.chat-example-answer summary').click();assert(await p.locator('.chat-example-answer').evaluate(e=>e.open));assert.match(await p.locator('.chatgpt-example').textContent(),/미리 작성한 예시/);assert(await p.locator('.chatgpt-example').evaluate(e=>e.scrollWidth<=e.clientWidth));console.log('PASS: ChatGPT practice example, reveal answer, clear example label, mobile width.');}finally{await b.close();}})().catch(e=>{console.error(e);process.exit(1)});
