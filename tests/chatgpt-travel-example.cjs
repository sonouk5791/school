const {chromium}=require('playwright'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:390,height:844}});await p.goto('http://127.0.0.1:8085');await p.evaluate(()=>{VoiceManager.isMuted=true;LessonEngine.startLesson('prompt_basic');});for(const i of [1,2]){await p.evaluate(i=>{LessonEngine.currentStepIndex=i;LessonEngine.renderCurrentStep();},i);await p.waitForFunction(()=>{const im=document.querySelector('.chat-travel-figure img');return im&&im.complete&&im.naturalWidth>0;});assert.match(await p.locator('.chat-example-question').textContent(),/2박 3일/);assert.equal(await p.locator('.chat-example-answer li').count(),3);assert(await p.locator('.chatgpt-example').evaluate(e=>e.scrollWidth<=e.clientWidth));}console.log('PASS: both travel stages show ChatGPT example with loaded itinerary image and responsive layout.');}finally{await b.close();}})().catch(e=>{console.error(e);process.exit(1)});
