const {chromium}=require('playwright'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage();await p.addInitScript(()=>{sessionStorage.setItem('school_character_welcome_v1','1');window.calls=0;speechSynthesis.speak=()=>window.calls++});await p.goto('http://127.0.0.1:8085/');await p.waitForFunction(()=>window.CharacterAudioPlayer);await p.evaluate(()=>CharacterAudioPlayer.refresh());assert(await p.locator('.character-greet').isDisabled());assert.equal(await p.locator('.character-audio-controls:visible').count(),0);assert.equal(await p.evaluate(()=>window.calls),0);console.log('PASS existing hero voice button disabled without recordings; no TTS, no extra visible controls');}finally{await b.close()}})().catch(e=>{console.error(e);process.exitCode=1});
