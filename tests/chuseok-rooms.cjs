const{chromium}=require('playwright'),assert=require('assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage();let errors=[];p.on('pageerror',e=>errors.push(e.message));await p.addInitScript(()=>{localStorage.setItem('season-preserve-test','unchanged');});for(const width of [1440,768,390,320]){await p.setViewportSize({width,height:1000});for(const [file,id,button] of [['senior-exercise.html','kongi','#btnStart'],['tori-play.html','tori','[data-game="match"]'],['nabi-learn.html','nabi','[data-learn="today"]'],['bori-hobby.html','bori','[data-tab="song"]']]){await p.goto('http://localhost:8085/'+file);await p.waitForTimeout(500);assert.equal(await p.getAttribute('body','data-season-room'),id);if(width===390)await p.screenshot({path:'tests/season-room-'+id+'-ready.png'});await p.locator(button).click();await p.waitForTimeout(300);assert(await p.locator('.season-room-companion').isVisible());assert.equal(await p.evaluate(()=>document.documentElement.scrollWidth>innerWidth+1),false,file+' '+width);assert.equal(await p.evaluate(()=>localStorage.getItem('season-preserve-test')),'unchanged');if(width===390)await p.screenshot({path:'tests/season-room-'+id+'-playing.png'});}}assert.deepEqual(errors,[]);await b.close();console.log('PASS room seasonal continuity, 16 viewports/states, companion visible, no overflow/errors, stored value preserved');})();

