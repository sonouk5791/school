const {chromium}=require('playwright'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage();await p.route('https://www.youtube.com/embed/**',()=>{});await p.goto('http://127.0.0.1:8085');await p.locator('#openJointRoom').click();await p.locator('[data-joint-video="1"]').click();await p.waitForFunction(()=>document.querySelector('.joint-loading')?.textContent.includes('연결이 늦어'),{},{timeout:12000});assert((await p.locator('.joint-loading a').getAttribute('href')).includes('7x9F-RQwypc'));await p.locator('#jointStop').click();assert.equal(await p.locator('.joint-loading').count(),0);console.log('PASS: stalled embed shows fallback with correct video link; stop clears loading state.');}finally{await b.close();}})().catch(e=>{console.error(e);process.exit(1)});
