const {chromium}=require('playwright'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage();await p.goto('http://127.0.0.1:8085');await p.evaluate(()=>{VoiceManager.isMuted=true;LessonEngine.startLesson('ai_writing');LessonEngine.currentStepIndex=2;LessonEngine.renderCurrentStep();});await p.waitForFunction(()=>{const i=document.querySelector('.chatgpt-example img');return i&&i.complete&&i.naturalWidth>0;});assert.match(await p.locator('.chat-example-question').textContent(),/공손하고 짧게/);assert.match(await p.locator('.chat-example-answer').textContent(),/늘 건강하세요/);console.log('PASS: writing example question, answer and image rendered.');}finally{await b.close();}})().catch(e=>{console.error(e);process.exit(1)});
