# 프로젝트 자동 저장 및 작업 관리 지침 (Project Automation Rules)

## 🧠 치매 어르신 인지 플랫폼 핵심 수정 원칙 (상시 자동 적용)
현재 홈페이지는 **디지털 AI 학교**이며, **치매 어르신들을 위한 인지 플랫폼**입니다.
모든 작업 시 사용자가 프롬프트에 별도로 적지 않아도 아래 6가지 원칙을 **상시 자동 적용**합니다:

1. **기존 자산 보존**: 기존에 만들어둔 기능, 페이지, 캐릭터, 콘텐츠, 동작 목록, 학습 데이터는 절대 임의로 삭제하지 않습니다.
2. **점진적 개선**: 전체를 갈아엎는 재구축이 아니라, 기존 구조를 면밀히 분석한 뒤 UI/UX, 레이아웃, 상태 흐름, 문구, 웹 접근성을 중심으로 개선합니다.
3. **어르신 맞춤형 UI/UX**: 주요 사용자는 치매 또는 노인성 질환이 있는 어르신이므로, 복잡한 구조, 작은 글씨(20px 이상 지향), 많은 버튼, 어려운 전문 용어, 이중 스크롤, 갑작스러운 화면 변화 및 팝업을 철저히 배제하고 최소화합니다.
4. **역할 및 화면 분리**: 사회복지사/관리자용 제어 기능은 어르신용 체험 화면과 명확히 분리합니다.
5. **일관성 유지**: 캐릭터 이름(콩이, 토리, 나비, 곰이/보리 등), 안내 문구, 디자인 톤앤매너, 페이지 구조를 서비스 전반에 걸쳐 일관성 있게 통일합니다.
6. **사전 영향도 분석**: 수정 전 반드시 관련 컴포넌트, CSS, 상태 로직을 먼저 분석한 뒤 기존 연계 기능이 깨지지 않도록 안전하게 작업합니다.

---

## 📌 핵심 원칙: 모든 작업의 자동 저장 및 기록 보장

이 프로젝트에서는 사용자의 요청에 따른 모든 개발/수정 작업이 완료될 때마다 **자동으로 작업 내용을 저장하고, 작업 일지에 기록하며, 배포용 릴리즈 폴더를 동기화**해야 합니다.


---

### 1. 작업 완료 시 자동 수행 체크리스트

모든 기능 추가, 버그 수정, 디자인/에셋 변경 작업이 끝난 직후 다음 절차를 **누락 없이 자동으로 수행**합니다:

1. **파일 변경사항 저장**:
   - 편집한 모든 소스 코드(HTML, CSS, JS) 및 이미지/미디어 자산이 디스크에 정상 저장되었는지 확인.
2. **오늘의 작업 일지 자동 업데이트**:
   - `오늘_작업_일지_YYYY-MM-DD.md` 및 `TODAY_WORK_REPORT.md` 파일에 새로 수행한 작업 항목(시간, 요청 내용, 작업 세부 내역, 변경된 파일 링크)을 누적 기록.
3. **릴리즈 디렉터리(`school-release/`) 자동 동기화**:
   - 루트의 변경 사항(`assets/`, `css/`, `js/`, `index.html`, `server.cjs`, `vercel.json` 등)을 `school-release/`에 즉시 복사하여 동기화 유지.
4. **Git 및 배포 상태 유지**:
   - 사용자가 원격 동기화나 배포를 원할 때 언제든지 즉시 배포될 수 있도록 빌드 및 테스트 상태를 무결점으로 유지.

---

### 2. 작업 일지 기록 표준 서식

작업 일지에 새 항목을 추가할 때는 다음 형식을 준수합니다:
- **작업 번호 / 시간**: 현재 시간 기준
- **사용자 요청 사항**: 요청의 핵심 내용
- **수행 내역**: 변경된 사항, 추가된 기능, 해결된 이슈
- **관련 파일**: clickable `file:///` 마크다운 링크 포함

### 자동 기록 실행 (2026-09-21)
- 작업 완료 시 `node scripts/save-work-report.cjs --title "작업 제목" --details "수행 내용 및 검증 결과"`를 실행해 오늘 일지와 TODAY_WORK_REPORT.md를 저장한다.
- Git 저장소는 `school-release/`이다. 저장소의 `core.hooksPath=.githooks` 설정으로 커밋 직전에 변경 파일 목록이 두 Markdown 일지에 자동 저장/스테이징된다.
- 새로 복제한 저장소에서는 `git config core.hooksPath .githooks`로 동일한 자동 기록을 켠다. 백그라운드 상시 감시나 예약 실행이 아니라 작업 완료/커밋 시 저장한다.

## 공식 캐릭터 기준 (2026-09-21 고해상도 요청으로 갱신)
- 최신 사용자 요청은 저해상도 시트 crop/확대를 중단하고 고해상도 재제작하는 것이다. 디자인 참고는 `characters/reference/high-resolution/design-reference.jpeg`이다.
- 행은 콩이/토리/나비/보리, 열은 아/에/이/오/우이다. 시트 내부 이름표 오기는 분류에 사용하지 않는다.
- 공식 고해상도 입모양은 `public/characters/{id}/mouth/{id}_{a,e,i,o,u}.png`이다. 캐릭터별 5장 사이에서는 입 밖의 얼굴/눈/안경/귀/머리/의상/몸 픽셀과 위치를 고정한다.
- 데이터는 `characters/manifest.json` 및 `js/characters.js`를 사용한다. mouth와 stable은 같은 고해상도 파일을 가리킨다. `_stable.png`는 이전 경로 호환용 복사본이다.
- 새 렌더는 내장 image_gen으로 제작하고 `scripts/build-hd-characters.cjs`로 입만 합성한다. 이전 `scripts/extract-official-characters.py`로 공식 파일을 덮어쓰지 않는다. 저해상도 시트 crop/업스케일을 최종 에셋으로 사용하지 않는다.
- 과거 이미지/영상/3D 자산은 기존 자료이며 공식 외형을 정의하지 않는다. 새 안내 캐릭터에는 공식 PNG 데이터만 사용한다.

- 동작 시트는 characters/reference/actions/source.png이며 공식 외형보다 우선하지 않는다. classification.json의 accepted만 사용하고 pose-only는 배포 캐릭터에 연결하지 않는다. 없는 동작은 같은 캐릭터 idle로 복귀한다.

## 사용자 지정 공통 작업 원칙 (2026-09-22, 상시 자동 적용)

이 절은 사용자가 직접 지정한 공통 규칙이다. 이후 작업에서 별도 재요청 없이 적용하며 임의로 변경하거나 삭제하지 않는다.

1. 현재 디지털 AI 학교 코드 구조를 먼저 분석하고 필요한 부분만 수정한다. 전체 프로젝트를 새로 만들거나 대규모로 재작성하지 않고 현재 구조를 최대한 재사용한다.
2. 콩이·토리·나비·보리의 기존 캐릭터 이미지와 디자인을 임의로 변경하지 않는다.
3. 기존 운동·놀이·학습·취미·기록·관리자 기능과 데이터를 보존한다. 중복 UI는 합치거나 이동할 수 있지만 실제 기능과 데이터는 삭제하지 않는다.
4. 기존 URL과 라우팅이 가능한 한 깨지지 않도록 한다.
5. 어르신 화면과 선생님/관리자 화면을 명확하게 구분한다.
6. 치매 및 고령층 사용자를 기준으로 큰 글씨, 큰 버튼, 짧은 문장, 단순한 선택, 높은 가독성, 중복 메뉴 최소화를 적용한다.
7. 수정 전에 관련 파일·컴포넌트·CSS·상태 관리·localStorage 등 저장 로직을 확인한다.
8. 수정 후 기존 기능이 깨지지 않았는지 확인한다. 가능한 경우 프로젝트에 마련된 build, lint, 기존 테스트 및 브라우저 콘솔 오류 점검을 수행한다. 실행하지 못한 검증을 통과했다고 보고하지 않는다.
9. 작업 완료 후 변경한 파일, 변경한 기능, 유지한 기존 기능, 확인이 필요한 부분을 짧게 보고한다.
10. 현재 사용자가 요청한 단계만 수행하며 다음 단계 작업까지 임의로 진행하지 않는다.

## 사용자 지정 보존 및 자동 배포 원칙 (2026-09-23, 상시 적용)

사용자가 지정한 공통 수정 규칙을 이후 작업에서도 자동 적용한다.

- 수정 전에 현재 코드, 관련 컴포넌트, CSS, 상태 흐름, localStorage 등 저장 로직을 분석하고 필요한 UI/UX와 화면 흐름만 점진적으로 수정한다. 전체 재구축 및 기존 기능·데이터 삭제는 금지한다.
- 콩이와 운동, 토리와 놀이, 나비와 학습, 보리와 취미, 오늘의 20분 활동, 캐릭터 집 꾸미기, 활동 기록, 선생님 공간, 기존 URL 및 페이지 연결을 유지한다. 기존 캐릭터 이미지와 디자인을 임의로 변경하지 않는다.
- 기존 Google Cloud / Gemini TTS 관련 코드·파일·환경변수·API 호출 구조와 캐릭터별 음성 연결 구조를 보존한다. 임의로 삭제하거나 browser speechSynthesis로 되돌리지 않는다. 환경변수 비밀값은 일지나 Git에 기록하지 않는다.
- 치매 및 고령층을 기준으로 큰 글씨와 버튼, 간단한 선택, 짧은 안내, 한 화면의 한 가지 주요 행동, 충분한 여백과 한국어 글자 깨짐 방지를 적용한다. 복잡한 메뉴와 갑작스러운 팝업은 최소화하고 어르신과 선생님/관리자 화면을 구분한다.
- 현재 요청에 포함된 단계만 수행한다. 변경 후 관련 기존 테스트와 가능한 검증을 실행하고, 미실행 검증을 통과했다고 보고하지 않는다.
- 사용자는 작업 완료 시 Markdown 자동 저장, GitHub 푸시 및 Vercel 배포를 명시적으로 요청했다. 이후 각 요청의 작업 완료 시 별도 재요청 없이 일지 저장 → 릴리즈 동기화 → 관련 검증 → Git 커밋 및 origin/main 푸시 → 기존 운영 Vercel 프로젝트 배포 및 상태 확인을 수행한다. 사용자가 특정 작업에서 푸시/배포를 제외하면 그 지시를 우선한다.
- 일지 저장은 `node scripts/save-work-report.cjs --title "작업 제목" --details "요청·수행 내용·변경 파일·검증 결과"`, 릴리즈 동기화는 `node scripts/sync-release.cjs`를 사용한다. Git 저장소와 기존 운영 Vercel 연결은 `school-release/`를 기준으로 확인한다.
- 자동 수행은 각 작업 완료 시 실행하며 백그라운드 예약 작업을 의미하지 않는다. 검증 실패 또는 인증·권한 문제 발생 시 완료로 기록하지 말고 원인을 보고한다. 최종 보고에는 변경 파일·기능, 보존한 기능, 검증 및 푸시·배포 결과를 짧게 남긴다.
